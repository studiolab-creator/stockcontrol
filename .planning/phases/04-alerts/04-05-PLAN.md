---
phase: 04-alerts
plan: "05"
type: execute
wave: 3
depends_on: ["04-00"]
files_modified:
  - src/app/(app)/productos/page.tsx
autonomous: true
requirements: [ALERT-05]

must_haves:
  truths:
    - "The /productos catalog table has a 'Stock' column showing current stock"
    - "Products with stock <= minStock show a 'Stock bajo' Badge variant=destructive"
    - "Products with stock > minStock show no badge — only the stock number"
    - "The badge uses stock <= minStock (NOT alertActive) per D-05"
    - "Stock display includes the unit when unidad is set: '{stock} {unidad}'"
    - "The new Stock column is positioned between Unidad and Stock mínimo"
    - "No query changes are needed — the existing findMany returns stock and minStock"
  artifacts:
    - path: "src/app/(app)/productos/page.tsx"
      provides: "Stock column with low-stock badge in product catalog table"
      contains: "Badge variant=\"destructive\""
  key_links:
    - from: "productos/page.tsx"
      to: "Badge component"
      via: "product.stock <= product.minStock conditional"
      pattern: "stock.*<=.*minStock"
---

<objective>
Add a Stock column with a low-stock badge to the /productos catalog table, following the exact DashboardClient pattern.

Purpose: ALERT-05 requires visual indicators in both /dashboard (already done) and /productos (missing). This plan adds the Stock column to the existing table with the same `stock <= minStock` threshold and `Badge variant="destructive"` used in DashboardClient.

Output: productos/page.tsx modified with Stock column; Badge already imported; no query change needed.
</objective>

<execution_context>
@/Users/joaquinrodriguez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaquinrodriguez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joaquinrodriguez/stockcontrol/.planning/ROADMAP.md
@/Users/joaquinrodriguez/stockcontrol/.planning/phases/04-alerts/04-UI-SPEC.md
@/Users/joaquinrodriguez/stockcontrol/.planning/phases/04-alerts/04-PATTERNS.md

<interfaces>
<!-- Current table structure in productos/page.tsx — columns to keep unchanged -->
Current columns: Nombre | SKU | Tipo | Categoría | Unidad | Stock mínimo | Acciones(admin-only)
Target columns:  Nombre | SKU | Tipo | Categoría | Unidad | Stock | Stock mínimo | Acciones(admin-only)

<!-- Badge is already imported in productos/page.tsx at line 13 -->
import { Badge } from '@/components/ui/badge'
// No new import needed.

<!-- isLowStock logic — from DashboardClient (d-05: INTENTIONALLY stock <= minStock, not <) -->
const isLowStock = product.stock <= product.minStock

<!-- Badge pattern from DashboardClient (dashboard-client.tsx lines 112-114) -->
{isLowStock && <Badge variant="destructive">Stock bajo</Badge>}

<!-- Stock display with unit pattern (dashboard-client.tsx lines 121-125) -->
{product.stock}{product.unidad ? ` ${product.unidad}` : ''}

<!-- Existing query already returns stock and minStock — NO QUERY CHANGE NEEDED -->
const products = await prisma.product.findMany({
  include: { categoria: true },
  orderBy: { nombre: 'asc' },
})
// The full Product model includes stock: Int and minStock: Int
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stock column with low-stock badge to /productos table</name>
  <files>src/app/(app)/productos/page.tsx</files>
  <read_first>
    - src/app/(app)/productos/page.tsx — read the FULL file to see the exact table structure before editing. Note: Badge is already imported. The table header row has TableHead elements that must be extended. The table body rows have TableCell elements that must be extended.
    - src/components/dashboard-client.tsx lines 49 and 112-114 — the isLowStock logic and Badge usage to replicate exactly
    - .planning/phases/04-alerts/04-UI-SPEC.md section "2. /productos page — Stock column addition" — full UI contract
    - .planning/phases/04-alerts/04-CONTEXT.md D-05 — stock <= minStock for visual (intentionally different from alert trigger)
  </read_first>
  <action>
Modify `src/app/(app)/productos/page.tsx` as follows. Make only the targeted changes described below — do not restructure or rewrite the file.

**Change 1 — Add Stock column header** in the `<TableHeader>` section.

Find this block inside TableHeader:
```tsx
            <TableHead>Unidad</TableHead>
            <TableHead>Stock mínimo</TableHead>
```

Change it to:
```tsx
            <TableHead>Unidad</TableHead>
            <TableHead>Stock</TableHead>
            <TableHead>Stock mínimo</TableHead>
```

(Insert `<TableHead>Stock</TableHead>` between Unidad and Stock mínimo)

**Change 2 — Add Stock column cell** in each `<TableRow>` inside `<TableBody>`.

Find this block inside the products.map():
```tsx
                <TableCell>{product.unidad ?? '—'}</TableCell>
                <TableCell>{product.minStock}</TableCell>
```

Change it to:
```tsx
                <TableCell>{product.unidad ?? '—'}</TableCell>
                <TableCell>
                  <div className="flex items-center gap-2">
                    <span>{product.stock}{product.unidad ? ` ${product.unidad}` : ''}</span>
                    {product.stock <= product.minStock && (
                      <Badge variant="destructive">Stock bajo</Badge>
                    )}
                  </div>
                </TableCell>
                <TableCell>{product.minStock}</TableCell>
```

**No other changes required:**
- The `prisma.product.findMany` query is UNCHANGED — it already returns `stock` and `minStock` via the full Product model.
- Badge is already imported at line 13 — no new import needed.
- The `isLowStock` condition is `product.stock <= product.minStock` — D-05 explicitly states this is `<=` (intentionally different from the alert trigger which uses `<`).

**Why `<=` and not `<`:**
D-05 is explicit: "Los indicadores visuales siguen basados en `stock <= minStock`, tal como está implementado en `DashboardClient` hoy." This is intentionally more inclusive than the email alert trigger (`stock < minStock`). The visual warns when stock is at or below the minimum; the email alerts only on crossing below.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
    <automated>grep -c "Stock bajo" /Users/joaquinrodriguez/stockcontrol/src/app/\(app\)/productos/page.tsx</automated>
    <automated>grep -c "stock.*<=.*minStock" /Users/joaquinrodriguez/stockcontrol/src/app/\(app\)/productos/page.tsx</automated>
    <automated>grep -c "TableHead.*Stock" /Users/joaquinrodriguez/stockcontrol/src/app/\(app\)/productos/page.tsx</automated>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/productos/page.tsx contains `<TableHead>Stock</TableHead>` (new column header)
    - File contains `product.stock <= product.minStock` (badge condition — using <=, not <)
    - File contains `Badge variant="destructive"` inside the products.map() table rows
    - File contains `"Stock bajo"` as the badge text
    - File contains `product.stock` display with unit pattern `{product.unidad ? \` \${product.unidad}\` : ''}`
    - File contains `flex items-center gap-2` wrapper div for stock + badge
    - The `prisma.product.findMany` query is unchanged (still uses `include: { categoria: true }`)
    - Badge import at top of file is unchanged (was already there)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>/productos table has Stock column with 'Stock bajo' badge for low-stock products; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → DB | getAuthenticatedUser() at top of ProductosPage — unauthenticated requests are redirected to /login |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | Stock data visible to all authenticated users | accept | All authenticated users (ADMIN + OPERADOR) can see the catalog and stock levels — this is intentional per the product design; stock visibility is not sensitive data in this internal tool |
| T-04-05-02 | Elevation of Privilege | Unauthenticated access to /productos | accept | Existing `getAuthenticatedUser()` at top of ProductosPage handles this — no change needed |
</threat_model>

<verification>
After task completes:
1. `grep "Stock bajo" src/app/(app)/productos/page.tsx` — returns match
2. `grep "stock <= product.minStock" src/app/(app)/productos/page.tsx` — returns match
3. `npx tsc --noEmit` — exits 0
4. `npm run build` — exits 0
5. Visual: navigate to /productos with a product whose stock <= minStock — "Stock bajo" badge appears in Stock column
</verification>

<success_criteria>
- /productos table shows Stock column between Unidad and Stock mínimo
- Products with stock <= minStock show "Stock bajo" Badge variant=destructive
- Products with stock > minStock show only the stock number, no badge
- `npx tsc --noEmit` and `npm run build` both pass
</success_criteria>

<output>
After completion, create `/Users/joaquinrodriguez/stockcontrol/.planning/phases/04-alerts/04-05-SUMMARY.md`
</output>
